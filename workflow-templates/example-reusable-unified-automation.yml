name: Example Calling Workflow

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

# Required permissions for reusable-unified-automation.yml
permissions:

jobs:
  call-unified-automation:
    uses: ./.github/workflows/reusable-unified-automation.yml
    with:
      # Add required inputs here
    secrets:
      # Add required secrets here
