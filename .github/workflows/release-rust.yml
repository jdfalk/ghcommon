# file: .github/workflows/release-rust.yml
# version: 1.9.2
# guid: b5c6d7e8-f9a0-1b2c-3d4e-5f6a7b8c9d0e

name: Rust Release Build

on:
  workflow_call:
    inputs:
      protobuf-artifacts:
        description: "Whether protobuf artifacts are available"
        required: false
        type: string
        default: "false"
      release-version:
        description: "Release tag (e.g., v1.2.3) used to align crate metadata"
        required: false
        type: string

permissions:
  contents: write
  packages: write

jobs:
  # Rust cross-platform build with comprehensive support
  build-rust:
    name: Build Rust (${{ matrix.target }})
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        include:
          - target: x86_64-unknown-linux-gnu
            os: ubuntu-latest
            name: linux-x86_64
            primary: true
          - target: aarch64-unknown-linux-gnu
            os: ubuntu-latest
            name: linux-arm64
            primary: false
          - target: x86_64-unknown-linux-musl
            os: ubuntu-latest
            name: linux-x86_64-musl
            primary: false
          - target: x86_64-pc-windows-msvc
            os: windows-latest
            name: windows-x86_64
            primary: false
    steps:
      - name: Checkout repository
        uses: actions/checkout@v5

      - name: Download protobuf artifacts
        if: inputs.protobuf-artifacts == 'true'
        uses: actions/download-artifact@v6
        with:
          name: protobuf-generated

      - name: Debug environment and detect version
        shell: bash
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          echo "🔍 Environment Debug Information"
          echo "================================="
          echo "Repository: ${{ github.repository }}"
          echo "Ref: ${{ github.ref }}"
          echo "Event: ${{ github.event_name }}"
          echo "Input release-version: '${{ inputs.release-version }}'"
          echo ""

          # Check if Cargo.toml exists and show its content
          if [[ -f "Cargo.toml" ]]; then
            echo "📋 Found existing Cargo.toml:"
            echo "---"
            head -10 Cargo.toml
            echo "---"
          else
            echo "❌ No Cargo.toml found - this might be the wrong repository type"
            echo "� Contents of current directory:"
            ls -la
            exit 1
          fi

          echo ""
          echo "�🔍 Detecting version using GitHub API..."

          # Try to get version from input first
          INPUT_VERSION="${{ inputs.release-version }}"
          echo "📥 Input version: '$INPUT_VERSION'"

          if [[ -n "$INPUT_VERSION" && "$INPUT_VERSION" != "--workspace" && "$INPUT_VERSION" != "" ]]; then
            VERSION="${INPUT_VERSION#v}"
            echo "✅ Using input version: $VERSION"
          else
            echo "🌐 Querying GitHub API for latest release..."

            # Get latest release from GitHub API with better error handling
            API_RESPONSE=$(curl -s -w "HTTPSTATUS:%{http_code}" -H "Authorization: token $GITHUB_TOKEN" \
              "https://api.github.com/repos/${{ github.repository }}/releases/latest")

            HTTP_STATUS=$(echo "$API_RESPONSE" | tr -d '\n' | sed -e 's/.*HTTPSTATUS://')
            BODY=$(echo "$API_RESPONSE" | sed -e 's/HTTPSTATUS\:.*//g')

            echo "📡 API HTTP Status: $HTTP_STATUS"

            if [[ "$HTTP_STATUS" == "200" ]]; then
              LATEST_RELEASE=$(echo "$BODY" | grep '"tag_name"' | cut -d'"' -f4 || echo "")

              if [[ -n "$LATEST_RELEASE" ]]; then
                # Increment patch version
                LATEST_VERSION="${LATEST_RELEASE#v}"
                echo "🏷️ Latest release: $LATEST_VERSION"

                # Parse version components
                IFS='.' read -r MAJOR MINOR PATCH <<< "$LATEST_VERSION"
                MAJOR=${MAJOR:-0}
                MINOR=${MINOR:-0}
                PATCH=${PATCH:-0}

                # Increment patch for new version
                NEW_PATCH=$((PATCH + 1))
                VERSION="$MAJOR.$MINOR.$NEW_PATCH"
                echo "🆙 Generated new version: $VERSION"
              else
                echo "⚠️ No tag_name found in API response"
                VERSION="0.1.0"
              fi
            else
              echo "⚠️ API call failed with status $HTTP_STATUS"
              if [[ "$HTTP_STATUS" == "404" ]]; then
                echo "📦 No releases found, starting with 0.1.0"
              fi
              VERSION="0.1.0"
            fi
          fi

          # Final validation and cleanup
          if [[ "$VERSION" == "--workspace" ]]; then
            echo "❌ CRITICAL: Version is '--workspace', this should never happen!"
            VERSION="0.1.0-emergency.$(date +%Y%m%d%H%M%S)"
          elif [[ -z "$VERSION" ]]; then
            echo "❌ CRITICAL: Version is empty!"
            VERSION="0.1.0-emergency.$(date +%Y%m%d%H%M%S)"
          elif ! echo "$VERSION" | grep -qE '^[0-9]+\.[0-9]+\.[0-9]+'; then
            echo "⚠️ Version '$VERSION' doesn't match semver, fixing..."
            VERSION="0.1.0-dev.$(date +%Y%m%d%H%M%S)"
          fi

          echo ""
          echo "✅ Final version: $VERSION"

          # Install cargo-set-version if needed
          if ! command -v cargo-set-version >/dev/null 2>&1; then
            echo "📦 Installing cargo-set-version..."
            cargo install cargo-set-version --locked
          fi

          # Set the version with explicit validation
          echo "🔧 Setting crate version to: $VERSION"

          # Method 1: Try cargo-set-version with correct syntax
          echo "🔍 Attempting Method 1: cargo set-version $VERSION"
          if cargo set-version "$VERSION"; then
            echo "✅ Method 1 successful"
          else
            echo "⚠️ Method 1 failed, trying Method 2"

            # Method 2: Use sed to directly update Cargo.toml
            echo "🔍 Method 2: Direct sed replacement"
            if [[ -f "Cargo.toml" ]]; then
              # Backup original
              cp Cargo.toml Cargo.toml.backup

              # Update version line
              sed -i.tmp "s/^version = .*/version = \"$VERSION\"/" Cargo.toml

              # Check if it worked
              if grep -q "^version = \"$VERSION\"" Cargo.toml; then
                echo "✅ Method 2 successful"
                rm -f Cargo.toml.tmp Cargo.toml.backup
              else
                echo "❌ Method 2 failed, restoring backup"
                mv Cargo.toml.backup Cargo.toml
                exit 1
              fi
            else
              echo "❌ No Cargo.toml found"
              exit 1
            fi
          fi

          # Verify the change
          echo ""
          echo "📋 Verifying Cargo.toml after version update:"
          if [[ -f "Cargo.toml" ]]; then
            echo "Version line:"
            grep "^version" Cargo.toml || echo "❌ No version line found!"
            echo ""
            echo "First 10 lines of Cargo.toml:"
            head -10 Cargo.toml
          fi
          echo "📋 Verifying Cargo.toml version:"
          if [[ -f "Cargo.toml" ]]; then
            grep "^version" Cargo.toml || echo "❌ No version line found in Cargo.toml"
          fi

      - name: Set up Rust
        uses: actions-rs/toolchain@v1
        with:
          toolchain: stable
          target: ${{ matrix.target }}
          override: true
          components: rustfmt, clippy

      - name: Install cross-compilation tools
        if: matrix.target == 'aarch64-unknown-linux-gnu'
        run: |
          sudo apt-get update
          sudo apt-get install -y gcc-aarch64-linux-gnu

      - name: Install musl tools
        if: matrix.target == 'x86_64-unknown-linux-musl'
        run: |
          sudo apt-get update
          sudo apt-get install -y musl-tools

      - name: Cache cargo registry
        uses: actions/cache@v4
        with:
          path: ~/.cargo/registry
          key: ${{ runner.os }}-cargo-registry-${{ matrix.target }}-${{ github.run_id }}
          restore-keys: |
            ${{ runner.os }}-cargo-registry-${{ matrix.target }}
            ${{ runner.os }}-cargo-registry

      - name: Cache cargo index
        uses: actions/cache@v4
        with:
          path: ~/.cargo/git
          key: ${{ runner.os }}-cargo-index-${{ matrix.target }}-${{ github.run_id }}
          restore-keys: |
            ${{ runner.os }}-cargo-index-${{ matrix.target }}
            ${{ runner.os }}-cargo-index

      - name: Cache cargo build
        uses: actions/cache@v4
        with:
          path: target
          key: ${{ runner.os }}-cargo-build-${{ matrix.target }}-${{ github.sha }}
          restore-keys: |
            ${{ runner.os }}-cargo-build-${{ matrix.target }}
            ${{ runner.os }}-cargo-build

      - name: Run Clippy
        if: matrix.primary
        run: cargo clippy --all-targets --all-features -- -D warnings

      - name: Run tests
        if: matrix.primary
        run: cargo test --verbose

      - name: Build release binary
        env:
          # Export cross-compilation environment variables for robustness
          CC: ${{ matrix.target == 'aarch64-unknown-linux-gnu' && 'aarch64-linux-gnu-gcc' || '' }}
          CARGO_TARGET_AARCH64_UNKNOWN_LINUX_GNU_LINKER: ${{ matrix.target == 'aarch64-unknown-linux-gnu' && 'aarch64-linux-gnu-gcc' || '' }}
          CARGO_TARGET_X86_64_UNKNOWN_LINUX_MUSL_LINKER: ${{ matrix.target == 'x86_64-unknown-linux-musl' && 'musl-gcc' || '' }}
        run: |
          cargo build --release --target ${{ matrix.target }}

      - name: Prepare binary (Unix)
        if: runner.os != 'Windows'
        run: |
          mkdir -p dist
          cp target/${{ matrix.target }}/release/$(basename $(pwd)) dist/$(basename $(pwd))-${{ matrix.name }}
          chmod +x dist/$(basename $(pwd))-${{ matrix.name }}

      - name: Prepare binary (Windows)
        if: runner.os == 'Windows'
        run: |
          mkdir dist
          copy target\\${{ matrix.target }}\\release\\$(Split-Path -Leaf $(Get-Location)).exe dist\\$(Split-Path -Leaf $(Get-Location))-${{ matrix.name }}.exe

      - name: Upload build artifacts
        uses: actions/upload-artifact@v5
        with:
          name: rust-${{ matrix.name }}
          path: dist/

  publish-rust-crate:
    name: Publish Rust Crate
    runs-on: ubuntu-latest
    needs: build-rust
    if: startsWith(github.ref, 'refs/tags/v')

    permissions:
      contents: read
      packages: write

    steps:
      - name: Checkout repository
        uses: actions/checkout@v5
        with:
          fetch-depth: 0

      - name: Set up Rust
        uses: actions-rs/toolchain@v1
        with:
          toolchain: stable
          override: true

      - name: Collect crate metadata
        id: crate-metadata
        run: |
          python3 .github/workflows/scripts/collect_rust_crate_metadata.py

      - name: Align crate version with release input
        id: resolved-version
        env:
          INPUT_RELEASE_VERSION: ${{ inputs.release-version }}
          CRATE_NAME: ${{ steps.crate-metadata.outputs.crate-name }}
          CURRENT_VERSION: ${{ steps.crate-metadata.outputs.crate-version }}
        run: |
          set -euo pipefail

          crate_name="${CRATE_NAME}"
          current_version="${CURRENT_VERSION}"
          target_version="${CURRENT_VERSION}"

          if [[ -n "${INPUT_RELEASE_VERSION}" ]]; then
            stripped="${INPUT_RELEASE_VERSION#v}"
            stripped="${stripped#V}"
            if [[ -n "${stripped}" ]]; then
              target_version="${stripped}"
            fi
          fi

          if [[ "${target_version}" != "${current_version}" ]]; then
            echo "Updating crate version from ${current_version} to ${target_version}"
            if ! command -v cargo-set-version >/dev/null 2>&1; then
              cargo install cargo-set-version --locked
            fi
            cargo set-version "${target_version}"
          else
            echo "Crate version already ${target_version}"
          fi

          tag_ref="${GITHUB_REF#refs/tags/}"
          tag_version="${tag_ref#v}"
          if [[ "${tag_version}" != "${target_version}" ]]; then
            echo "::warning::Git tag (${tag_version}) does not match crate version (${target_version})"
          else
            echo "Tag matches crate version (${tag_version})"
          fi

          {
            echo "CRATE_NAME=${crate_name}"
            echo "CRATE_VERSION=${target_version}"
          } >> "${GITHUB_ENV}"

          echo "crate-name=${crate_name}" >> "${GITHUB_OUTPUT}"
          echo "crate-version=${target_version}" >> "${GITHUB_OUTPUT}"
          echo "tag-version=${tag_version}" >> "${GITHUB_OUTPUT}"

      - name: Configure Cargo for GitHub Packages
        env:
          CARGO_REGISTRY_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          python3 .github/workflows/scripts/configure_cargo_registry.py

      - name: Check if version already published
        id: check-published
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          OWNER: ${{ github.repository_owner }}
        run: |
          set -euo pipefail

          crate="${CRATE_NAME}"
          version="${CRATE_VERSION}"
          owner="${OWNER}"

          already="false"
          resolved_scope="unknown"

          for scope in orgs users; do
            api_url="https://api.github.com/${scope}/${owner}/packages/cargo/${crate}/versions"
            echo "Checking ${api_url}..."

            response="$(curl -sS -H "Authorization: Bearer ${GITHUB_TOKEN}" \
              -H "Accept: application/vnd.github+json" -w "HTTPSTATUS:%{http_code}" "${api_url}" || true)"

            http_status="${response##*HTTPSTATUS:}"
            body="${response%HTTPSTATUS:*}"

            if [[ "${http_status}" == "200" ]]; then
              resolved_scope="${scope}"
              versions="$(echo "${body}" | jq -r '.[].name // empty')"
              if [[ -n "${versions}" ]]; then
                echo "Found published versions:"
                echo "${versions}" | head -10 | sed 's/^/  - /'
                if echo "${versions}" | grep -qx "${version}"; then
                  echo "⚠️  Version ${version} already published"
                  already="true"
                fi
              else
                echo "No versions reported yet."
              fi
              break
            elif [[ "${http_status}" == "404" ]]; then
              echo "Package not found under ${scope}. Trying next scope..."
            else
              echo "::warning::Unexpected response (${http_status}) while checking ${scope} scope"
            fi
          done

          echo "already-published=${already}" >> "${GITHUB_OUTPUT}"
          echo "registry-scope=${resolved_scope}" >> "${GITHUB_OUTPUT}"

      - name: Publish crate to GitHub Packages
        if: steps.check-published.outputs.already-published != 'true'
        env:
          CARGO_REGISTRY_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          set -euo pipefail

          echo "📤 Publishing ${CRATE_NAME}@${CRATE_VERSION} to GitHub Package Registry"
          cargo publish \
            --registry github \
            --verbose \
            --allow-dirty \
            --no-verify
          echo "✅ Publish command completed"

      - name: Verify publication
        if: steps.check-published.outputs.already-published != 'true'
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          OWNER: ${{ github.repository_owner }}
        run: |
          set -euo pipefail

          crate="${CRATE_NAME}"
          owner="${OWNER}"
          scope="${{ steps.check-published.outputs.registry-scope }}"

          if [[ "${scope}" == "unknown" ]]; then
            scopes=(orgs users)
          else
            scopes=("${scope}")
          fi

          for attempt in {1..5}; do
            for sc in "${scopes[@]}"; do
              api_url="https://api.github.com/${sc}/${owner}/packages/cargo/${crate}"
              echo "Attempt ${attempt}/5 checking ${api_url}"

              response="$(curl -sS -H "Authorization: Bearer ${GITHUB_TOKEN}" \
                -H "Accept: application/vnd.github+json" -w "HTTPSTATUS:%{http_code}" "${api_url}" || true)"

              http_status="${response##*HTTPSTATUS:}"
              body="${response%HTTPSTATUS:*}"

              if [[ "${http_status}" == "200" ]]; then
                echo "✅ Package visible in registry (${sc})"
                echo "Package details:"
                echo "${body}" | jq -r '"  Name: \(.name)\n  Visibility: \(.visibility)\n  URL: \(.html_url)"'
                exit 0
              fi
            done

            if [[ "${attempt}" -lt 5 ]]; then
              echo "Package not visible yet. Waiting 10 seconds..."
              sleep 10
            fi
          done

          echo "::warning::Package ${crate} not yet visible via API. It may take a few minutes to propagate."

      - name: Skip publication (already published)
        if: steps.check-published.outputs.already-published == 'true'
        run: |
          echo "ℹ️  Skipping publish because ${CRATE_NAME}@${CRATE_VERSION} already exists."
          echo "Check https://github.com/${{ github.repository }}/packages for details."

      - name: Create publication summary
        if: always()
        run: |
          status="${{ job.status }}"
          already="${{ steps.check-published.outputs.already-published }}"

          {
            echo "# 📦 Rust Crate Publication"
            echo ""
            echo "- **Crate**: \`${CRATE_NAME}\`"
            echo "- **Version**: \`${CRATE_VERSION}\`"
            echo "- **Repository**: \`${{ github.repository }}\`"
            echo "- **Tag**: \`${GITHUB_REF#refs/tags/}\`"
            echo ""
            if [[ "${already}" == "true" ]]; then
              echo "**Status:** ⏭️ Skipped (version already published)"
            elif [[ "${status}" == "success" ]]; then
              echo "**Status:** ✅ Published to GitHub Packages"
            else
              echo "**Status:** ❌ Publication failed"
            fi
            echo ""
            echo "## 🔗 Useful Links"
            echo "- Package listing: https://github.com/${{ github.repository }}/packages"
            echo "- Registry index: https://api.github.com/${{ github.repository }}/cargo/"
            echo "- Release tag: https://github.com/${{ github.repository }}/releases/tag/${GITHUB_REF#refs/tags/}"
            echo ""
            echo "## 📚 Consuming the crate"
            echo "\`\`\`toml"
            echo "${CRATE_NAME} = \"${CRATE_VERSION}\""
            echo "\`\`\`"
            echo ""
            echo "Configure \`.cargo/config.toml\`:"
            echo "\`\`\`toml"
            echo "[registries.github]"
            echo "index = \"sparse+https://api.github.com/${{ github.repository }}/cargo/\""
            echo "\`\`\`"
          } >> "${GITHUB_STEP_SUMMARY}"
